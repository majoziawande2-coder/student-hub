# PMB Student Hub

GitHub Pages-ready static version of the PMB Student Hub project.

## GitHub Pages

Enable GitHub Pages from **Settings → Pages** and select **Deploy from a branch**, then choose `main` and `/ (root)`.

The site entry point is `index.html`.
